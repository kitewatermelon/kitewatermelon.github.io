---
title: "[논문리뷰] LeJEPA: Provable and Scalable Self-Supervised Learning Without the Heuristics (1)"
last_modified_at: 2026-06-08
layout: single
categories:
  - Paper-Review
tags:
  - Computer-Vision
  - Self-Supervised-Learning
  - JEPA
excerpt: "LeJEPA: Provable and Scalable Self-Supervised Learning Without the Heuristics (1)"
use_math: true
classes: wide
---
>  NeurIPS 2022 [[Paper](https://arxiv.org/pdf/2511.08544)] [[GitHub](https://github.com/rbalestr-lab/lejepa)]  
>  Randall Balestriero, Yann LeCun
>  14 Nov 2025

## 들어가며
LeJEPA는 Brown Univ.의 Randall Balestriero 교수와 AI의 대부인 Yann LeCun 교수가 다양한 downstream task에도 강건한 모델을 만들기 위해 모델 출력의 임베딩이 따라야하는 분포가 Isotropic Gaussian임을 이론적으로 증명하고, 그 분포를 따르도록 하는 SigReg라는 regulaization을 제안한 논문이다. 

필자는 이를 보다 높은 해상도로 이해하고 싶은 욕심이 있기에 다음과 같은 구성을 결심했다.

(1)편에서 `1 Introduction ~ 2 Background and Notations` 을 통해 배경 지식을 정리할 것이다.

(2)편에서 `3 Latent Euclidean: Embeddings  Should be Isotropic Gaussian` 을 깊게 공부하여 왜 임베딩이 Isotropic Gaussian이 되어야 하는지 증명할 것이다.

(3)편에서 `4 SIGReg: Reliable Isotropic  Gaussian Regularization in High-Dimension`을 통해 어떻게 LeJEPA가 model의 출력을 Isotropic Gaussian으로 강제하는지 알아볼 것이다.

(4)편에서 `5 LeJEPA: Stable and Scalable  Implementation`가 어떻게 구현되는지 확인 할 것이다.

(5)편에서 `6 LeJEPA: Empirical Validation ~ 7 Conclusion`을 통해 LeJEPA가 실제로 의미가 있는지 알아볼 것이다.


## 1 Introduction
세계와 그 역학의 조작 가능한 representation을 학습하는 것은 AI 분야에서 오랜 질문으로, 그 기원은 수세기 전으로 거슬러 올라간다. 이미지 인식, 로봇 공학, 물리학, 우주 탐사와 같은 여러 분야에서 공통된 질문은 `관찰을 통해 조직적이고 실행 가능한 고차원 임베딩 공간을 어떻게 학습할 것인가? `이다.

파라미터화된 비선형 오퍼레이터인 $f_\theta$를 사용하여 관측치를 임베딩으로 매핑하는 것은 이 퍼즐의 표준적인 첫 조각이다. 그 다음 퍼즐은 아직 표준화가 덜 된 어떻게 $f_\theta$를 학습할 것인가? 이다.

Joint-Embedding Predictive Architectures (JEPAs)는 $f_\theta$를 의미론적으로 관련있는 두 `views`간 predictive agreement를 최대화하여 $f_\theta$를 학습시키도록 제안한다.


> 여기서 `views`란 transformations과 corruptions라는 두가지 폼이 존재한다.  
> 결국엔 입력 이미지를 masking, cropping, blurring, temporal or spatial translations, geometric or photometric transformations, viewpoint changes, views from different sensor modalities, etc. 하는 것이다. 

어떤 경우든간에 `views`는 두 개가 의미적으로 어느 정도 연관되어 있어야 prediction task가 인코더 $f_\theta$의 임베딩을 데이터 속에 숨겨진 진짜 지식 쪽으로 정렬시킬 수 있다.

그런데 JEPA의 prediction task는 잘못하면 모델이 다 똑같은 벡터를 뱉어버리는 collapse를 일으키게 된다. identical embeddings (complete collapse)나 low dimensional subspace (dimensional collapse)

> 이를 출력하면 모델 입장에서 가장 쉬운 해답이 되기 때문인데 이는 뭐가 들어오든 같은 벡터를 뱉으면 prediction loss가 0이 되기 때문이다.

이를 해결하기 위해 SOTA 방법은 휴리스틱에 의존하게 된다. 대표적으로 stop-gradient, 비대칭 뷰 생성 , EMA 스케줄링을 통한 teascher-student network, explicit한 normalization, whitening layers 도입 등이 있으며 이들은 모두 하이퍼파라미터를 조금만 바꿔도 collapse가 나거나 성능이 떨어진다. 저자들은 이론은 안 파고 스케일만 키우고 있음을 지적하며 이론적 뒷받침에 대한 필요를 설명한다. 

저자들은 묻고 답한다.
> JEPA가 준수해야 할 필요 조건이 무엇일까?   
> (i) prediction task를 풀되, (기존의 JEPA)   
> (2) 임베딩의 분포를 등방성 가우스 분포를 강제하는 것이다. (SIGReg)

저자들은 임베딩의 분포를 등방성 가우스 분포를 강제하기 위해 SIGReg(Sktched Isotropic Gaussian Regularization)를 제안한다.

SIGReg는 (1) 통계적으로 보장되었으며 (2) 기존의 휴리스틱들을 사용하지 않아도 되고 (3) 메모리와 계산 복잡성이 선형이며 (4) collapse 방지를 위한 하이퍼파라미터는 오직 하나만 있어도 되는 장점이 있다.

